name: My Awesome Hello World Workflow

on: [push]

jobs:
  say-hello-and-check-contents:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Say Hello
      run: echo "👋 Hello, GitHub Actions!"

    - name: List repo files
      run: |
        cd poker-hand-calculator
        ls -la

      
